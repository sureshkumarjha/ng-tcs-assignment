import { CursorStyleDirective } from './cursor-style.directive';

describe('CursorStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CursorStyleDirective();
    expect(directive).toBeTruthy();
  });
});
